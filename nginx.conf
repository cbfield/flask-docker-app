server {
    listen 80;
    server_name docker.bucketmeadow.com www.docker.bucketmeadow.com;

    location ~ /.well-known/acme-challenge {
        allow all;
        root /usr/share/nginx/html;
    }

    root /usr/share/nginx/html;
    index index.html;

    # location / {
    #     include uwsgi_params;
    #     uwsgi_pass flask_app:8080;
    # }
}